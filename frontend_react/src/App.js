// import { useRef } from "react";
import React, { useState, useEffect, useContext } from "react";
import "./appCss.css";

function App() {
  return <></>;
}

export default App;
// const [inputValue, setInputValue] = useState("");
// const count = useRef(0);

// useEffect(() => {
//   count.current = count.current + 1;
// });
//  let [mycolor, setMyColor] = useState({ color: "red", colorName: "redColor" });

//  function changeColor() {
//    if (mycolor.color === "red")
//      setMyColor({ color: "green", colorName: "greenColor" });
//    else {
//      setMyColor({ color: "red", colorName: "redColor" });
//    }
//  }
